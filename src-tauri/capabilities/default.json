{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "Main window: core APIs and events for response streaming.",
  "windows": ["main"],
  "permissions": ["core:default", "core:event:default"]
}
